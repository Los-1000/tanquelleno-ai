# Lunetra IA — dependencias de la Fase M (Modelado)
# Instalación:  pip install -r requirements.txt
# Probado con Python 3.12

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
pyarrow>=14.0        # escritura del dataset maestro en parquet
openpyxl>=3.1        # lectura del histórico en Excel
matplotlib>=3.7      # figuras del perfilado (analisis/perfilado_datos.py)
pytest>=7.4          # suite del contrato de modelado
